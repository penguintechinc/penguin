//! Privileged daemon + service (systemd/launchd/SCM) subcommands.
//!
//! Skeleton binary — implementation lands in its migration milestone.
fn main() {
    // Intentionally empty in M0; wired up in later milestones.
}
