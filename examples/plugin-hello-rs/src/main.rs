//! Example external plugin written in Rust against penguin-sdk.
//!
//! Used as a conformance fixture: it must load under the frozen Go daemon
//! (reverse go-plugin direction). Skeleton in M0; implemented in M3.
fn main() {
    // Intentionally empty in M0.
}
