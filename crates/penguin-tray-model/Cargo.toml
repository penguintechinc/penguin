[package]
name = "penguin-tray-model"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
# The ergonomic module/health/command types this crate turns into a menu.
# Deliberately the *only* dependency: no GUI toolkit, no gRPC transport, no
# I/O — the platform shells (M7) own that and hand this crate already-typed
# data.
penguin-sdk.workspace = true
