# Vendored hashicorp/go-plugin protos (MPL-2.0)

`grpc_broker.proto`, `grpc_controller.proto`, `grpc_stdio.proto` are vendored
verbatim (with their MPL-2.0 headers) from hashicorp/go-plugin@v1.7.0 in **M1**,
when `penguin-proto` codegen is wired up. Kept out of M0 to keep the scaffold
dependency-free. Recorded in LICENSE-THIRD-PARTY.
