# OS
.DS_Store
Thumbs.db
*~

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Build artifacts
bin/
# Binaries accidentally built into the repo root (build to ./bin via `make build`)
/penguind
/penguin
/penguin-tray
/pcli
/plugin-hello
*.exe
*.msi
coverage.filtered.out
dist/
coverage.out
coverage.unit.out
coverage.int.out
coverage.intfiltered.out
*.test

# Secrets & env — never commit
.env
.env.*
*.key
*.pem
credentials.json
# Root-anchored: ignore a top-level real-secrets dir only. A bare `secrets/`
# also matched the internal/secrets source package and left it untracked.
/secrets/

# Local state
.PLAN
.TODO
