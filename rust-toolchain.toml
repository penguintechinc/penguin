# Pinned toolchain for the penguin Rust workspace (reproducible builds).
# 1.97 matches the org container standard (rust:1.97-slim-bookworm).
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy", "llvm-tools-preview"]
