version: "2"

linters:
  default: standard
  enable:
    - gosec
    - ineffassign
    - unused
    - misspell
    - unconvert
    - prealloc
  settings:
    gosec:
      excludes: []

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
