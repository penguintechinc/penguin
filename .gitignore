# Rust workspace
/target/
**/*.rs.bk

# Coverage (cargo-llvm-cov)
*.profraw
*.profdata
lcov.info
coverage/

# OS / editors
.DS_Store
Thumbs.db
*~
*.swp
*.swo
.idea/
.vscode/

# Secrets & env — never commit (root-anchored so it never shadows source dirs)
.env
.env.*
*.key
*.pem
credentials.json
/secrets/

# Local crash-recovery state
.PLAN
.TODO

# Parallel-work git worktrees (this repo's checkout is shared with other
# sessions; the migration work runs in an isolated worktree here)
/.worktrees/
.worktrees/
